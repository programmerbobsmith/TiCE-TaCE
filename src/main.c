#include <stdint.h>
#include <graphx.h>

//function prototypes
void x_draw(unsigned int x, unsigned int y);
void y_draw(unsigned int x, unsigned int y);
void board_draw(unsigned int x, unsigned int y);

int main()
{
	gfx_ZeroScreen();
	gfx_Line(
	return 0;
}

void board_draw(unsigned int x, unsigned int y);
{
	gfx_SetColor(0);
	gfx_Horizontal(
}